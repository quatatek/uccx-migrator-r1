UCCX Migrator
This tool helps you migrate configuration from one UCCX Cluster to another.

Use Cases:
1. Update to new cluster with configuration migration.
2. Lab recreate

Instead of in-place upgrade, you may decide to build new cluster. This is due to aging VMWare infrastructure, avoid multi-stage upgrade.

This tool will assist you migrating configuration from old cluster to new.
